services:
  - type: web
    name: orbitops
    runtime: docker

    healthCheckPath: /api/health

    envVars:
      - key: WEB_CONCURRENCY
        value: "1"

      - key: GEMINI_API_KEY
        sync: false

      - key: CORS_ALLOWED_ORIGINS
        sync: false
