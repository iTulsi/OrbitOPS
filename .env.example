# OrbitOPS server configuration
PORT=5050
WEB_CONCURRENCY=1

# Add the real value only inside your local .env file
GEMINI_API_KEY=

# Comma-separated frontend origins allowed to call the API
CORS_ALLOWED_ORIGINS=http://localhost:5050,http://localhost:5173
