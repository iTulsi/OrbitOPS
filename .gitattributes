# Normalize repository text files across macOS, Linux, and Windows.
* text=auto eol=lf

# Generated Vite production bundles may contain unavoidable whitespace.
frontend/dist/** -whitespace

# Windows command files require CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
